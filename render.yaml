services:
  - type: web
    name: farm-survey-app
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false  # Set this manually in Render dashboard with your Neon connection string

